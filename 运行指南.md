python3 -m venv venv_clean --without-pip  # 先创建无 pip 环境避免污染
source venv_clean/bin/activate

重装pytorch
# 激活虚拟环境
source /home/lienne/edge-intelligent-traffic/backend/venv_clean/bin/activate

# 卸载现有 torch 和 torchvision
pip uninstall torch torchvision -y

# 重新安装（根据你的 CUDA 版本选择，例如 CUDA 11.8）
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118

# 若无 GPU 或不确定 CUDA 版本，直接安装 CPU 版本（可先测试）
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu


# 创建环境并安装
conda create -n torch_env python=3.10 -y
conda activate torch_env
conda install pytorch torchvision pytorch-cuda=11.8 -c pytorch -c nvidia -y
python -c "import torch; print(torch.cuda.is_available())"

# 安装 Miniconda (若未安装)
wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh
bash Miniconda3-latest-Linux-x86_64.sh -b -p $HOME/miniconda3
source $HOME/miniconda3/bin/activate

onda tos accept --override-channels --channel https://repo.anaconda.com/pkgs/main
conda tos accept --override-channels --channel https://repo.anaconda.com/pkgs/r
source $HOME/miniconda3/bin/activate
# 创建并激活环境
conda create -n torch_env python=3.10 -y
conda activate torch_env

# 安装 PyTorch (CUDA 11.8 版本)
conda install pytorch torchvision pytorch-cuda=11.8 -c pytorch -c nvidia -y

~/miniconda3/envs/torch_env/bin/python train_eurosat.py


📋 第四步：导出环境配置，方便未来一键复现

将当前完美的环境保存为配置文件，以后重装系统或分享给同事时，一条命令即可复原：
bash

conda activate torch_env
conda env export --no-builds > ~/torch_env_perfect.yml

未来在新机器上复原环境只需：
bash

conda env create -f ~/torch_env_perfect.yml

cd backend/
python model/train.py